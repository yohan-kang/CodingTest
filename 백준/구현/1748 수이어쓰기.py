# 숫자를 문자열로 입력받음 (예: "120")
n = input()

 # N의 자릿수 - 1 (예: 120이면 length = 2)
length = len(n) - 1
 
result = 0

# 자릿수가 낮은 숫자들의 개수를 먼저 계산하는 부분
for i in range(length):
    result += 9 * (10**i) * (i+1)


	•	9 * (10^i) * (i+1)
	•	9 × (10의 거듭제곱) × (자릿수 개수)
	•	예제:
	•	i=0 → 9 × (10^0) × (0+1) = 9
	•	i=1 → 9 × (10^1) × (1+1) = 180
	•	i=2 → 9 × (10^2) × (2+1) = 2700
   
# 마지막 자릿수의 개수를 계산하는 부분
result += ((int(n) - (10 ** length))+1) * (length+1)

print(result)